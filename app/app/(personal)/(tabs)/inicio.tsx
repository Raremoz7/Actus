import { DashboardScreen } from '@/components/dashboard';

// Aba Início do personal — dashboard compartilhado (escolhe conteúdo por tipo).
export default function PersonalInicioScreen() {
  return <DashboardScreen />;
}
