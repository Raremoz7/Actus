import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 7 (desafios do personal).
export default function PersonalDesafiosScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Desafios</AppText>
    </Screen>
  );
}
