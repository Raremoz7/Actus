import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 9 (perfil do personal).
export default function PersonalPerfilScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Perfil</AppText>
    </Screen>
  );
}
