import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 9 (montagem de treinos).
export default function PersonalTreinosScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Treinos</AppText>
    </Screen>
  );
}
