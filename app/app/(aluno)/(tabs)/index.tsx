import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 3 (HOJE).
export default function AlunoHojeScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Hoje</AppText>
    </Screen>
  );
}
