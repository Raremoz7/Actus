import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 4 (treinos do aluno).
export default function AlunoTreinosScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Treinos</AppText>
    </Screen>
  );
}
