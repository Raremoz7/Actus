import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 8 (dietas).
export default function NutriDietasScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Dietas</AppText>
    </Screen>
  );
}
