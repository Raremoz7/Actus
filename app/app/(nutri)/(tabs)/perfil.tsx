import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 9 (perfil do nutricionista).
export default function NutriPerfilScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Perfil</AppText>
    </Screen>
  );
}
