import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 1 (gate de troca de senha).
export default function TrocarSenhaScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Trocar senha</AppText>
    </Screen>
  );
}
