import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 1.
export default function ProfessorInfoScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Informações do profissional</AppText>
    </Screen>
  );
}
