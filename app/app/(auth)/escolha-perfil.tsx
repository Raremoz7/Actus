import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 1.
export default function EscolhaPerfilScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Escolha de perfil</AppText>
    </Screen>
  );
}
