import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 1 (dados pessoais).
export default function Passo2VoceScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Passo 2 · Você</AppText>
    </Screen>
  );
}
