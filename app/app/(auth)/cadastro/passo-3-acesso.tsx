import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 1 (e-mail + senha).
export default function Passo3AcessoScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Passo 3 · Acesso</AppText>
    </Screen>
  );
}
