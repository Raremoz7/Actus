import { Screen, AppText } from '@/components/ui';

// Placeholder — implementação real no Bloco 1 (validação do convite).
export default function Passo1ConviteScreen() {
  return (
    <Screen padded>
      <AppText variant="h2">Passo 1 · Convite</AppText>
    </Screen>
  );
}
